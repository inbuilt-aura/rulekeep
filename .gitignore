node_modules/
dist/
# The plugin ships its own bundled copy — it must be committed, or an
# installed plugin has no executable to run (docs/04-build-plan.md M3).
!plugins/claude-code/dist/
*.log
.DS_Store

# Written by `claude plugin eval` (docs/05-testing.md).
evals/results/
